{
  "name": "@f/insert-element",
  "description": "Insert an element at the specified position within a container",
  "repository": "git://github.com/micro-js/insert-element.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "hihat": "^2.5.0",
    "tape": "^4.2.2"
  }
}
